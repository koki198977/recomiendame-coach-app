// Re-exporta desde el contexto global para mantener compatibilidad
export { usePlan, PlanProvider } from '../contexts/PlanContext';
export type { PlanType, FeatureKey, FeatureStatus } from '../contexts/PlanContext';
